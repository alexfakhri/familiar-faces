<%= form_for @article, html: {multipart: true} do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :story %>
  <%= f.text_area :story %>
  <%= f.file_field :photo %>
  <%= f.submit "Update Story" %>
<% end %>
