Admin Panel

<% if @articles.all.any? %>
  <% @articles.all.each do |article| %>
    <h2><%= link_to article.title, article_path(article) %></h2>
  <% end %>
<% else %>
  <h1>No articles yet</h1>
<% end %>
